---
title: "Isoform switching — exploratory report"
format:
  html: default
execute:
  echo: true
  warning: true
  message: true
---

# Purpose

Optional scratch space. The main **overview + QC report** is `isoform_switching_overview.qmd` (run `quarto render reports/isoform_switching_overview.qmd` after `01`, `02`, `02b`, `02c`).

```{r}
#| label: setup
#| eval: false
# Example: from project root, `source("utils/bootstrap.R")` and readRDS processed tables
```

# Next steps

- Add chunks that load `data/processed/isoformFeatures_*.rds` and `results/tables/gene_level_summary_*.rds`
- Add figures and session info when `06_visualization.R` is in place
